The service-log export at `/app/workflow/export_report.py` is broken. Build `/app/log_audit.py` to diagnose it from `/app/incident/export_dossier.md` and repair it against `/app/data/events.json`, `/app/data/silence_windows.json`, and `/app/data/service_dependencies.json`. The complete behavioral and structured-output contract is `/app/docs/report_spec.json`; its `contract_version` is document metadata, while `summary.json.schema_version` must match the separate value defined by the output contract.

Implement the exact `diagnose` and `repair` interfaces in the spec. Diagnosis `input_stats` has exactly `event_count`, `unique_event_ids`, and `services`; its services use the canonical aliases `api-gw`/`api gateway`→`api`, `database`→`db`, and `worker-batch`→`worker`. Evidence must use verbatim dossier excerpts and literal substrings copied from `/app/workflow/.export_report.original`, never paraphrases or patched-source text. Preserve that frozen file. Both modes must include every allowed issue ID. Diagnose mode omits `verified_summary` and `output_paths`; repair mode writes `summary.json`, `service_matrix.json`, `flagged.jsonl`, `diagnosis.json`, and `repair_audit.json` beneath the requested output directory and supports the default `/app/output`.

Treat every schema list and ordering rule in the spec as normative. `verified_summary` is a complete copy of `summary.json`; `repair_audit.json.processing_steps` equals the spec array verbatim; token maps use exact literal token keys; and `post_repair` contains both `flagged_count` and `rerun_flagged_count`. Capture all pre-repair fields from the frozen original before changing the workflow. The repaired executable must contain every `workflow_repair.required_executable_tokens` literal, including `reverse=True`.

Every `repair` call is self-contained: verification may restore the broken snapshot over `/app/workflow/export_report.py` immediately before invoking repair, with either the default or a custom output directory. Therefore repair must reinstall the corrected workflow from the frozen evidence on every invocation, run it using the requested output directory, and rerun it for idempotency; it must never depend on a prior patch performed by `solve.sh` or an earlier command.

The processing roadmap is canonicalize and deduplicate events, compact and apply service/level silence windows, compute silence pressure, propagate stateful dependency pressure, then build deterministic summary, service-matrix, and compact flagged outputs. The repaired pipeline must support `--input` and `--output-dir` and derive all results from the supplied data rather than fixtures.

Dependency pressure is stateful: process eligible flagged rows in chronological `(ts_ms, id)` order and finish each row’s pressure before it can be used as a source for later rows. For each source event, choose only its maximum matching-rule contribution; then rank distinct source events, retain at most three, and sum them. The worked examples in `report_spec.json.service_dependencies` are part of the contract and disambiguate max-rule deduplication, top-three selection, and recursive propagation.

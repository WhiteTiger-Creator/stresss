export is broken again — `/app/workflow/export_report.py` is not producing flagged output that matches what ops expects from `/app/data/events.json`.

months of incident notes are in `/app/incident/export_dossier.md` (long-context archive, at least 500 lines). most of it is noise and some early triage threads contradict later findings — treat the dossier as the source of truth for *behavior*, not every bridge-shift blurb. **All output schemas, audit fields, and acceptance constraints are defined in `/app/docs/report_spec.json`** — match that spec exactly.

build `/app/log_audit.py` with `diagnose` and `repair` subcommands per spec. `diagnose` reads `/app/data/events.json` for `input_stats` (no separate `--input` flag on diagnose), writes a JSON report from `--dossier` / `--report`, and must **not** include `verified_summary` or `output_paths`. `repair` patches `/app/workflow/export_report.py` in place and writes outputs under `--output-dir` (default `/app/output`).
`input_stats.services` must be canonicalized with the same service normalization/alias rules used by repaired pipeline outputs (`api-gw`/`api gateway` -> `api`, `database` -> `db`, `worker-batch` -> `worker`), then sorted ascending.
`input_stats` keys must be exactly `event_count`, `unique_event_ids`, and `services`.
silence windows are always loaded from `/app/data/silence_windows.json`.

non-negotiable silence canonicalization rules (repeat from spec so they are visible without deep file scrolling):
- valid `level_scope` values are only `all`, `warn`, `error`; drop any row with other scopes (for example `debug`) **before** silence compaction and checksum computation.
- silence interval compaction must merge touching intervals; after sorting by `(start_ms, end_ms)`, merge when `next.start_ms <= current.end_ms` (not only strict overlap).

keep `/app/workflow/.export_report.original` read-only and untouched. diagnosis evidence must cite that frozen snapshot (literal substrings) and verbatim dossier excerpts — not paraphrases and not text from already-patched code. `issues_found` must include **every** allowed issue id from the spec on both diagnose and repair runs. for each issue, evidence fields must satisfy the substring terms in `required_terms_by_issue` inside `/app/docs/report_spec.json`.

minimum evidence cues by issue (see spec for the exact contract):
- `wrong_timestamp_field`: mention `ts_ms`; include pipeline token `event["timestamp"]`
- `severity_filter`: mention `warn`; include pipeline token `level == "error"`
- `sort_order`: mention both `ascending` and `descending` recency/sort behavior
- `level_normalization`: mention both `WARN` and `lowercase` normalization
- `dedupe_policy`: mention duplicate-id collapse with newest `ts_ms`
- `suppressed_filter`: mention suppressed rows excluded from flagged export

after repair, write under the output dir: `summary.json`, `service_matrix.json`, `flagged.jsonl`, `diagnosis.json`, and `repair_audit.json`. in repair mode, `diagnosis.json` must include repaired-only keys `verified_summary` and `output_paths` (diagnose mode omits them). `verified_summary` must be a complete copy of `summary.json` (all summary keys present with identical values, including `raw_event_count`). `output_paths` key names are exact: `summary_json`, `flagged_jsonl`, `service_matrix_json`. `flagged.jsonl` must use compact JSON (`json.dumps(..., separators=(",", ":"))`, no space after `:`). `repair_audit.json` required keys include at minimum `patched_workflow`, `processing_steps`, `removed_tokens`, `pre_repair`, and `post_repair`; `patched_workflow` must be exactly `/app/workflow/export_report.py`. `repair` must always regenerate and patch `/app/workflow/export_report.py` from the frozen original snapshot path contract, even if the current workflow file was reset to the broken version before invoking `repair`. `repair_audit.json` must record `pre_repair` **before** patching, use the exact `processing_steps` list from the spec, and map each forbidden executable token string to booleans in `removed_tokens` and `pre_repair.pipeline_tokens_present`. those two maps must use the exact literal token strings as keys (`event["timestamp"]` and `level == "error"`), not semantic labels such as `reads_timestamp_field` or `critical_only_filter`. `pre_repair.pipeline_source_sha256` must be computed from `/app/workflow/.export_report.original` before any write to `/app/workflow/export_report.py`. evidence fields (`dossier_quote`, `pipeline_evidence`, `repair_action`) must satisfy term contracts and be meaningful snippets (minimum lengths in spec). the patched pipeline must remove forbidden tokens from executable code and include required repair markers from the spec. rerunning the patched pipeline with the same input must be idempotent and produce byte-identical logical outputs.

version fields are intentionally distinct: the spec document uses `contract_version` for its own revision, while `summary.json.schema_version` must be exactly `"service-log-report-v2"` as defined at `outputs.summary_json.schema_version`.

canonicalization per spec: normalize level and service via `str(...).strip().lower()`, apply service aliases (`api-gw`/`api gateway` -> `api`, `database` -> `db`, `worker-batch` -> `worker`), normalize `ts_ms` with `int(str(...).strip())` (invalid -> `0`), normalize message by collapsing internal whitespace, normalize suppressed strings (`true/1/yes`, `false/0/no`), dedupe by event id keeping **highest ts_ms** (tie-break by severity rank `error>warn>info>debug`, then prefer non-suppressed rows, then lexicographically greater normalized message, then normalized service), and count `total_events` from canonical deduped rows (suppressed rows still counted in totals). canonical row order is explicit: after dedupe, sort canonical rows by `ts_ms` ascending (stable for ties). before flagged export, canonicalize silence windows from `/app/data/silence_windows.json` (normalize service aliases + level scope + start/end int coercion, drop rows where `level_scope` is not one of `all|warn|error`, drop `end_ms <= start_ms`, compact overlapping or touching intervals per `(service, level_scope)` using merge rule `next.start_ms <= current.end_ms`). flagged export includes warn+error, excludes suppressed, excludes rows silenced by matching silence windows (`level_scope` of `all` or exact level) using `start_ms <= ts_ms < end_ms`, and for each included row compute `silence_pressure_score` from probe window `[ts_ms-90, ts_ms+1)` as `(all_overlap_ms // 25) + (level_overlap_ms // 15)` where overlaps are against compacted silence intervals for `(service, all)` and `(service, level)` respectively. each flagged row must include `event_digest`: first 10 lowercase hex chars of SHA1 over `id|ts_ms|level|service|message|silence_pressure_score`. final flagged sort order is exact: `ts_ms` descending, severity rank descending, `silence_pressure_score` descending, then `id` ascending. `summary.json` must include **all required fields** from spec, including at minimum `raw_event_count`, `unique_event_ids`, `total_events`, `level_counts`, `services`, `flagged_count`, `suppressed_excluded_count`, `silence_excluded_count`, `canonical_fingerprint`, `silence_compaction_checksum`, `max_silence_pressure_score`, and `flagged_digest_checksum`. `canonical_fingerprint` is SHA1 over canonical rows in canonical order using exactly `id|ts_ms|level|service|message|suppressed_int` joined by newline, where `suppressed_int` is `1` for true and `0` for false. `silence_compaction_checksum` is SHA256 over compacted silence rows in canonical order `service|level_scope|start_ms|end_ms`. `flagged_digest_checksum` is SHA256 over `|`-joined `event_digest` values in final flagged order. compute all summary, matrix, and flagged values from input JSON and silence windows — do not hard-code verifier numbers. patched pipeline must accept `--input` and `--output-dir` and work with alternate input files the verifier supplies.

dependency extension (authoritative where it extends the preceding baseline): load `/app/data/service_dependencies.json` and canonicalize/dedupe rules exactly as `service_dependencies` defines in the spec. compute causal pressure only across final unsuppressed, unsilenced warn/error rows. for each target, score strictly earlier source events through matching lag rules, retain the three strongest sources under the specified tie-breaks, and emit `dependency_pressure_score` plus ordered `dependency_source_ids`. the extended `event_digest` input is `id|ts_ms|level|service|message|silence_pressure_score|dependency_pressure_score|comma_joined_dependency_source_ids`. the final flagged sort is `ts_ms` descending, severity rank descending, dependency pressure descending, silence pressure descending, id ascending. summary additionally requires `dependency_compaction_checksum` and `max_dependency_pressure_score`. all exact formulas and boundary conditions remain normative in `/app/docs/report_spec.json`.

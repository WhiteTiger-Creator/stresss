{
  "title": "Service Log Export \u2014 Diagnosis & Report Spec",
  "contract_version": "4.3",
  "known_issues": [
    {
      "id": "wrong_timestamp_field"
    },
    {
      "id": "severity_filter"
    },
    {
      "id": "sort_order"
    },
    {
      "id": "level_normalization"
    },
    {
      "id": "dedupe_policy"
    },
    {
      "id": "suppressed_filter"
    }
  ],
  "quick_contract_index": {
    "required_issue_ids": [
      "wrong_timestamp_field",
      "severity_filter",
      "sort_order",
      "level_normalization",
      "dedupe_policy",
      "suppressed_filter"
    ],
    "issue_item_shape": {
      "top_level": [
        "id",
        "severity",
        "description",
        "resolution",
        "evidence"
      ],
      "nested_evidence": [
        "dossier_quote",
        "pipeline_evidence",
        "repair_action"
      ],
      "minimum_lengths": [
        30,
        10,
        10
      ]
    },
    "evidence_term_checklist": {
      "field_order": [
        "dossier_quote",
        "pipeline_evidence",
        "repair_action"
      ],
      "wrong_timestamp_field": [
        [
          "ts_ms"
        ],
        [
          "event[\"timestamp\"]"
        ],
        [
          "ts_ms"
        ]
      ],
      "severity_filter": [
        [
          "warn"
        ],
        [
          "level == \"error\""
        ],
        [
          "warn"
        ]
      ],
      "sort_order": [
        [
          "ascending",
          "descending"
        ],
        [
          "sort",
          "ts_ms"
        ],
        [
          "descending"
        ]
      ],
      "level_normalization": [
        [
          "WARN",
          "lowercase"
        ],
        [
          "level"
        ],
        [
          "lower"
        ]
      ],
      "dedupe_policy": [
        [
          "duplicate",
          "ts_ms"
        ],
        [
          "for event in events"
        ],
        [
          "duplicate"
        ]
      ],
      "suppressed_filter": [
        [
          "suppressed",
          "excluded"
        ],
        [
          "suppressed"
        ],
        [
          "suppressed"
        ]
      ],
      "matching": "dossier_quote and repair_action terms are case-insensitive; pipeline_evidence terms are case-sensitive and the complete evidence value must be a literal substring of the frozen source"
    },
    "diagnose_input_stats_exact_keys": [
      "event_count",
      "unique_event_ids",
      "services"
    ],
    "repair_output_files": [
      "summary.json",
      "service_matrix.json",
      "flagged.jsonl",
      "diagnosis.json",
      "repair_audit.json"
    ],
    "repair_output_path_keys": {
      "summary_json": "<output-dir>/summary.json",
      "flagged_jsonl": "<output-dir>/flagged.jsonl",
      "service_matrix_json": "<output-dir>/service_matrix.json"
    },
    "repair_audit_exact_keys": [
      "patched_workflow",
      "processing_steps",
      "removed_tokens",
      "pre_repair",
      "post_repair"
    ],
    "repair_audit_post_repair_exact_keys": [
      "flagged_count",
      "rerun_flagged_count"
    ],
    "repair_audit_field_shapes": {
      "patched_workflow": "plain JSON string equal to /app/workflow/export_report.py; never an object",
      "pre_repair": "object with pipeline_source_sha256 and pipeline_tokens_present",
      "post_repair": "object with integer flagged_count and rerun_flagged_count"
    },
    "processing_steps_contract": "copy repair_audit.processing_steps verbatim, preserving exact strings and order",
    "absolute_policy_sources": {
      "events_default": "/app/data/events.json",
      "silence_windows": "/app/data/silence_windows.json",
      "service_dependencies": "/app/data/service_dependencies.json",
      "resolution": "--input replaces only events_default; policy paths remain absolute and are never resolved relative to --input or --output-dir"
    },
    "silence_scope_allowlist": {
      "allowed": [
        "all",
        "warn",
        "error"
      ],
      "drop_before": [
        "interval_compaction",
        "silence_compaction_checksum"
      ],
      "explicitly_invalid_examples": [
        "debug",
        "info"
      ]
    },
    "pipeline_public_interface": "python3 /app/workflow/export_report.py --input PATH --output-dir PATH",
    "internal_python_api": "not prescribed; helper function names, parameters, and return values are implementation details",
    "read_in_full": "This file is the complete output contract; truncated reads miss required schema sections. Read it completely before implementing.",
    "verified_summary_required_fields": [
      "schema_version",
      "raw_event_count",
      "unique_event_ids",
      "total_events",
      "level_counts",
      "services",
      "flagged_count",
      "suppressed_excluded_count",
      "silence_excluded_count",
      "canonical_fingerprint",
      "silence_compaction_checksum",
      "dependency_compaction_checksum",
      "max_silence_pressure_score",
      "max_dependency_pressure_score",
      "max_causal_burst_score",
      "max_dependency_chain_depth",
      "max_blast_radius_score",
      "blast_radius_digest_checksum",
      "lineage_digest_checksum",
      "flagged_digest_checksum"
    ]
  },
  "cli": {
    "path": "/app/log_audit.py",
    "subcommands": {
      "diagnose": {
        "usage": "python3 /app/log_audit.py diagnose --dossier PATH --report PATH",
        "defaults": {
          "dossier": "/app/incident/export_dossier.md",
          "report": "/app/output/diagnosis.json"
        },
        "must_include_all_allowed_ids": true,
        "must_not_include_repaired_keys": [
          "verified_summary",
          "output_paths"
        ],
        "input_events_path": "/app/data/events.json"
      },
      "repair": {
        "usage": "python3 /app/log_audit.py repair --output-dir PATH",
        "defaults": {
          "output_dir": "/app/output"
        },
        "output_dir_contract": "all five repaired artifacts must be written under the provided --output-dir path; when a custom path is supplied, do not create, rewrite, or update any artifact under /app/output, including /app/output/diagnosis.json",
        "required_output_files": [
          "summary.json",
          "service_matrix.json",
          "flagged.jsonl",
          "diagnosis.json",
          "repair_audit.json"
        ]
      }
    }
  },
  "diagnosis_report": {
    "required_keys_diagnosed": [
      "pipeline_status",
      "issues_found",
      "input_stats"
    ],
    "required_keys_repaired": [
      "pipeline_status",
      "issues_found",
      "input_stats",
      "verified_summary",
      "output_paths"
    ],
    "pipeline_status_diagnosed": "diagnosed",
    "pipeline_status_repaired": "repaired",
    "issues_found_item": {
      "required_fields": [
        "id",
        "severity",
        "description",
        "resolution",
        "evidence"
      ],
      "allowed_ids": [
        "wrong_timestamp_field",
        "severity_filter",
        "sort_order",
        "level_normalization",
        "dedupe_policy",
        "suppressed_filter"
      ],
      "evidence": {
        "required_fields": [
          "dossier_quote",
          "pipeline_evidence",
          "repair_action"
        ],
        "pipeline_evidence_source": "/app/workflow/.export_report.original",
        "pipeline_evidence_format": "literal substring excerpt from frozen original workflow source",
        "dossier_quote_format": "verbatim excerpt from /app/incident/export_dossier.md (whitespace-normalized match)",
        "minimum_lengths": {
          "dossier_quote": 30,
          "pipeline_evidence": 10,
          "repair_action": 10
        },
        "term_matching": "dossier_quote and repair_action term checks are case-insensitive; pipeline_evidence term checks are case-sensitive literal substrings",
        "required_terms_by_issue": {
          "wrong_timestamp_field": {
            "dossier_quote": [
              "ts_ms"
            ],
            "pipeline_evidence": [
              "event[\"timestamp\"]"
            ],
            "repair_action": [
              "ts_ms"
            ]
          },
          "severity_filter": {
            "dossier_quote": [
              "warn"
            ],
            "pipeline_evidence": [
              "level == \"error\""
            ],
            "repair_action": [
              "warn"
            ]
          },
          "sort_order": {
            "dossier_quote": [
              "ascending",
              "descending"
            ],
            "pipeline_evidence": [
              "sort",
              "ts_ms"
            ],
            "repair_action": [
              "descending"
            ]
          },
          "level_normalization": {
            "dossier_quote": [
              "WARN",
              "lowercase"
            ],
            "pipeline_evidence": [
              "level"
            ],
            "repair_action": [
              "lower"
            ]
          },
          "dedupe_policy": {
            "dossier_quote": [
              "duplicate",
              "ts_ms"
            ],
            "pipeline_evidence": [
              "for event in events"
            ],
            "repair_action": [
              "duplicate"
            ]
          },
          "suppressed_filter": {
            "dossier_quote": [
              "suppressed",
              "excluded"
            ],
            "pipeline_evidence": [
              "suppressed"
            ],
            "repair_action": [
              "suppressed"
            ]
          }
        }
      }
    },
    "input_stats": {
      "required_fields": [
        "event_count",
        "unique_event_ids",
        "services"
      ],
      "exact_key_names": "input_stats keys must be exactly event_count, unique_event_ids, services",
      "services_sort": "ascending alphabetical",
      "services_normalization": "services must use the same canonicalization as canonicalization.service_normalization (strip/lower + aliases); raw unnormalized spellings are invalid",
      "source_events_path": "/app/data/events.json"
    },
    "verified_summary": {
      "completeness_contract": "verified_summary must be a full key-for-key copy of summary.json in repair mode; omissions (for example raw_event_count) are invalid",
      "required_fields": [
        "schema_version",
        "raw_event_count",
        "unique_event_ids",
        "total_events",
        "level_counts",
        "services",
        "flagged_count",
        "suppressed_excluded_count",
        "silence_excluded_count",
        "canonical_fingerprint",
        "silence_compaction_checksum",
        "dependency_compaction_checksum",
        "max_silence_pressure_score",
        "max_dependency_pressure_score",
        "max_causal_burst_score",
        "max_dependency_chain_depth",
        "max_blast_radius_score",
        "blast_radius_digest_checksum",
        "lineage_digest_checksum",
        "flagged_digest_checksum"
      ]
    },
    "output_paths": {
      "summary_json": "<output-dir>/summary.json",
      "flagged_jsonl": "<output-dir>/flagged.jsonl",
      "service_matrix_json": "<output-dir>/service_matrix.json",
      "key_naming_contract": "keys are semantic snake_case names, not filenames with dots"
    },
    "top_level_key_policy": "required_keys_diagnosed and required_keys_repaired are EXACT top-level key sets, not minimums. Do not add extra top-level keys (for example a separate `status` field alongside pipeline_status) in either mode.",
    "pipeline_status_note": "pipeline_status values are exact short literals: `diagnosed` after diagnose, `repaired` after repair. Do not substitute descriptive sentences."
  },
  "canonicalization": {
    "silence_compaction_checksum_worked_example": {
      "source": "/app/data/silence_windows.json",
      "notes": [
        "api gateway and api-gw normalize to api, and their touching/overlapping warn intervals merge to [1700000008500,1700000008700)",
        "debug scope is dropped because it is unsupported",
        "search start_ms='oops' coerces to 0, so [0,10) is valid and retained"
      ],
      "exact_sha256_payload": "api|warn|1700000008500|1700000008700\ndb|all|1700000005800|1700000006100\nsearch|warn|0|10\nworker|warn|1700000002400|1700000002600\nworker|warn|1700000003900|1700000004050",
      "expected_sha256": "44005a978bedd93d1a55e22d0d37319c1fde585855ac7ee6274230f425af621f"
    },
    "event_digest": "first 10 lowercase SHA1 hex chars of id|ts_ms|level|service|message|silence_pressure_score|dependency_pressure_score|comma_joined_dependency_source_ids|dependency_chain_depth|causal_burst_score|comma_joined_dependency_lineage_ids|lineage_digest|blast_radius_score|comma_joined_blast_radius_services|blast_radius_digest; this is the only event_digest definition",
    "event_digest_worked_example": {
      "exact_utf8_payload": "e1|100|error|api|bad state|2|7|s0|1|12|s0|abcdef123456|4|db,worker|1234567890",
      "expected_first_10_sha1": "b251c997eb",
      "delimiter_contract": "literal pipe between fields, comma inside ordered lists, no trailing delimiter"
    }
  },
  "repair_audit": {
    "required_keys": [
      "patched_workflow",
      "processing_steps",
      "removed_tokens",
      "pre_repair",
      "post_repair"
    ],
    "processing_steps": [
      "record_pre_repair_sha256",
      "normalize_level",
      "normalize_service",
      "normalize_service_aliases",
      "normalize_ts_ms",
      "normalize_message",
      "dedupe_by_id",
      "normalize_suppressed",
      "load_silence_windows",
      "apply_silence_windows",
      "compute_silence_pressure",
      "load_service_dependencies",
      "compute_dependency_pressure",
      "compute_dependency_lineage",
      "compute_causal_burst",
      "compute_blast_radius",
      "build_lineage_digest",
      "build_blast_radius_digest",
      "build_event_digest",
      "filter_suppressed",
      "build_flagged"
    ],
    "patched_workflow": "/app/workflow/export_report.py",
    "patched_workflow_contract": {
      "type": "string",
      "exact_value": "/app/workflow/export_report.py",
      "not_allowed": "object, map, or nested path descriptor"
    },
    "forbidden_executable_tokens": [
      "event[\"timestamp\"]",
      "level == \"error\""
    ],
    "removed_tokens_schema": {
      "description": "Map each forbidden_executable_tokens entry to a boolean (true when absent from patched executable code, excluding comments and docstrings).",
      "required_key_source": "keys must be the exact literal strings from forbidden_executable_tokens (no semantic aliases)",
      "required_keys_exact": [
        "event[\"timestamp\"]",
        "level == \"error\""
      ],
      "example_valid": {
        "event[\"timestamp\"]": true,
        "level == \"error\"": false
      },
      "example_invalid": {
        "reads_timestamp_field": true,
        "critical_only_filter": false
      }
    },
    "pre_repair": {
      "required_fields": [
        "pipeline_source_sha256",
        "pipeline_tokens_present"
      ],
      "field_names_contract": "use these names exactly; frozen_source, sha256, source_sha256, or other aliases are invalid",
      "capture_order": "before any write to export_report.py",
      "capture_source": "/app/workflow/.export_report.original",
      "capture_rule": "pipeline_source_sha256 and pipeline_tokens_present are computed from the frozen original snapshot bytes before applying any patch to /app/workflow/export_report.py",
      "pipeline_tokens_present_schema": {
        "description": "Map each forbidden_executable_tokens entry to a boolean (true when present in frozen broken snapshot before repair).",
        "required_key_source": "keys must be the exact literal strings from forbidden_executable_tokens (no semantic aliases)",
        "required_keys_exact": [
          "event[\"timestamp\"]",
          "level == \"error\""
        ]
      }
    },
    "post_repair": {
      "required_fields": [
        "flagged_count",
        "rerun_flagged_count"
      ],
      "field_contract": "flagged_count is summary.json.flagged_count from the repaired run; rerun_flagged_count is the second idempotency run's summary.json.flagged_count"
    }
  },
  "workflow_repair": {
    "path": "/app/workflow/export_report.py",
    "cli_usage": "python3 /app/workflow/export_report.py --input PATH --output-dir PATH",
    "public_contract": "only the CLI and generated files are contractual; an internal export_report function is optional, and no internal function signature or return type is prescribed",
    "repair_state_contract": {
      "self_contained_invocation": "every invocation of `python3 /app/log_audit.py repair --output-dir PATH` must install a correct repaired implementation at /app/workflow/export_report.py before executing it",
      "starting_state": "the current /app/workflow/export_report.py may contain the frozen broken implementation, a previous repair, or arbitrary stale contents; repair must not assume solve.sh or an earlier repair already patched it",
      "reset_scenario": "verification may copy /app/workflow/.export_report.original over /app/workflow/export_report.py immediately before invoking repair, including when a custom --output-dir is used",
      "required_sequence": [
        "read and capture pre-repair evidence from /app/workflow/.export_report.original",
        "write or generate the repaired executable at /app/workflow/export_report.py",
        "run that repaired executable with the requested output directory",
        "run it again for idempotency verification"
      ],
      "frozen_snapshot_rule": "never modify /app/workflow/.export_report.original",
      "output_directory_rule": "--output-dir changes where the five output artifacts are written; it does not change the workflow path that must be repaired",
      "custom_output_isolation": "when --output-dir is not /app/output, write no files under /app/output and leave every pre-existing default-output artifact byte-for-byte unchanged"
    },
    "defaults": {
      "input": "/app/data/events.json",
      "output_dir": "/app/output",
      "silence_windows": "/app/data/silence_windows.json"
    },
    "forbidden_executable_tokens": [
      "event[\"timestamp\"]",
      "level == \"error\""
    ],
    "implementation_freedom": "variable names, constants, helper structure, and sorting syntax are not contractual; verification asserts generated behavior and files",
    "forbidden_token_check_scope": "executable code only (exclude comments and docstrings)"
  },
  "outputs": {
    "summary_json": {
      "schema_version": "service-log-report-v2",
      "level_counts_key_order": [
        "debug",
        "error",
        "info",
        "warn"
      ],
      "canonical_fingerprint": "sha1 over canonical rows in canonical_order joined by newline where each row is 'id|ts_ms|level|service|message|suppressed_int' and suppressed_int is 1 or 0",
      "silence_excluded_count": "number of canonical warn/error rows excluded by silence windows (after suppressed exclusion)",
      "silence_compaction_checksum": "sha256 over compacted silence rows in canonical order (service, level_scope, start_ms, end_ms) as lines service|level_scope|start_ms|end_ms",
      "dependency_compaction_checksum": "sha256 over canonical dependency rules in order (upstream_service, downstream_service, lag_min_ms, lag_max_ms), as newline-joined upstream_service|downstream_service|lag_min_ms|lag_max_ms|weight",
      "max_silence_pressure_score": "maximum silence_pressure_score across final flagged rows (0 when there are no flagged rows)",
      "max_dependency_pressure_score": "maximum dependency_pressure_score across final flagged rows (0 when there are no flagged rows)",
      "max_causal_burst_score": "maximum causal_burst_score across final flagged rows (0 when there are no flagged rows)",
      "max_dependency_chain_depth": "maximum dependency_chain_depth across final flagged rows (0 when there are no flagged rows)",
      "max_blast_radius_score": "maximum blast_radius_score across final flagged rows (0 when there are no flagged rows)",
      "blast_radius_digest_checksum": "sha256('|'.join(blast_radius_digest values in final flagged order)) lowercase 64 hex chars",
      "lineage_digest_checksum": "sha256('|'.join(lineage_digest values in final flagged order)) lowercase 64 hex chars",
      "flagged_digest_checksum": "sha256('|'.join(event_digest values in final flagged order)) lowercase 64 hex chars"
    },
    "service_matrix_json": {
      "description": "The JSON root is the service map itself, with no schema_version field, services wrapper, metadata wrapper, or other envelope. Each top-level key is a canonical service name and each value is that service's level-count object.",
      "top_level_shape": "{canonical_service: {debug: int, error: int, info: int, warn: int}}",
      "service_sort": "ascending alphabetical",
      "level_key_order": [
        "debug",
        "error",
        "info",
        "warn"
      ],
      "exact_nested_keys": true
    },
    "flagged_jsonl": {
      "required_fields": [
        "id",
        "ts_ms",
        "level",
        "service",
        "message",
        "silence_pressure_score",
        "dependency_pressure_score",
        "dependency_source_ids",
        "dependency_chain_depth",
        "dependency_lineage_ids",
        "causal_burst_score",
        "blast_radius_services",
        "blast_radius_score",
        "blast_radius_digest",
        "lineage_digest",
        "event_digest"
      ],
      "sort": "ts_ms descending with tiebreaks severity rank desc, causal_burst_score desc, blast_radius_score desc, dependency_pressure_score desc, dependency_chain_depth desc, silence_pressure_score desc, id asc",
      "levels_included": [
        "warn",
        "error"
      ],
      "exclude_suppressed": true,
      "json_format": "compact (no space after colon)"
    }
  },
  "long_context": {
    "dossier_path": "/app/incident/export_dossier.md",
    "minimum_line_count": 500
  },
  "silence_windows": {
    "source_path": "/app/data/silence_windows.json",
    "row_fields": [
      "service",
      "level_scope",
      "start_ms",
      "end_ms"
    ]
  },
  "service_dependencies": {
    "source_path": "/app/data/service_dependencies.json",
    "row_fields": [
      "upstream_service",
      "downstream_service",
      "lag_min_ms",
      "lag_max_ms",
      "weight"
    ],
    "lineage_digest": "first 12 lowercase SHA256 hex chars of id|dependency_chain_depth|causal_burst_score|comma_joined_dependency_lineage_ids",
    "blast_radius_digest": "first 10 lowercase SHA1 hex chars of service|comma_joined_blast_radius_services|blast_radius_score",
    "event_digest_reference": "use canonicalization.event_digest; there is no baseline/extended digest variant",
    "worked_example_max_rule_and_top_three": {
      "severity_rank": {
        "error": 4,
        "warn": 3
      },
      "canonical_rules": [
        "api->worker lag [50,200) weight 4 (a duplicate weight-2 rule was discarded)",
        "db->worker lag [50,100) weight 3"
      ],
      "chronological_sources": [
        "s1: api/error at 100, dependency_pressure_score 0",
        "s2: api/warn at 110, dependency_pressure_score 0",
        "s3: db/error at 120, dependency_pressure_score 0"
      ],
      "target": "s4: worker/error at 200",
      "contributions": [
        "s1=4*4+0+(0//2)=16",
        "s2=4*3+0+(0//2)=12",
        "s3=3*4+0+(0//2)=12"
      ],
      "retained_source_ids": [
        "s1",
        "s3",
        "s2"
      ],
      "dependency_pressure_score": 40
    },
    "worked_example_recursive_propagation": {
      "rules": [
        "api->worker weight 4",
        "worker->db weight 2"
      ],
      "rows": [
        "a: api/error has dependency score 0, depth 0, lineage [], burst 0",
        "b: worker/error receives 4*4+0+(0//2)=16 from a; depth 1; lineage [a]; burst=min(16+(0//2)+3,9999)=19",
        "c: db/error receives 2*4+0+(16//2)=16 from b; depth 2; lineage [b,a]; burst=min(16+(19//2)+6,9999)=31"
      ],
      "clarification": "b's finalized dependency, depth, lineage, and burst values are used when b contributes to c; rows are not computed independently from an all-zero snapshot"
    }
  },
  "semantics_source": {
    "notes": "This document is the output contract only: CLI shapes, report schemas, exact key sets, digest payload encodings, checksum serialization, and pinned worked examples. It does NOT define how values are derived.",
    "authority": "Normalization, dedupe tie-breaks, silence scopes/windows/pressure, dependency matching, contribution, lineage, causal burst, blast radius, and flagged ordering were settled during the service review; the authoritative record is the #SVC-ticketed decision notes inside /app/incident/export_dossier.md. Where an early proposal or interim position conflicts with a later decision, the later decision wins."
  }
}
